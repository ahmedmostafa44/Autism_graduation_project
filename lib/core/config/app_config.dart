class AppConfig {
  /// Set to true to bypass Firebase Auth and use mock credentials.
  /// This is useful for development when the Firebase project is not yet set up.
  static const bool useMockAuth = false;
}
